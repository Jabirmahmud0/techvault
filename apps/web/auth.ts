/**
 * Auth configuration placeholder.
 * 
 * This project uses a custom JWT auth system via the Express backend.
 * Authentication state is managed by Zustand (auth-store.ts).
 * Refresh tokens are stored as httpOnly cookies by the backend.
 * 
 * This file is kept for reference — NextAuth is NOT used.
 */

export { };
